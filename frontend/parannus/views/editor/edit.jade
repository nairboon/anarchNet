form(method="post",action="update")
        p
         label(for="type") Branch:
            input(type="text", name="branch", value=item.head)
            
        p
         label(for="content") Data:
            textarea(type="text",rows="20", name="content")
                #{item.content}
        p.buttons
          input(type="submit", value="Save")
